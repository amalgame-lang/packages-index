# Amalgame — official package index
#
# Curated by Bastien Mouget. `amc package add <shortname>@<tag>`
# looks up this file via raw.githubusercontent.com to resolve the
# shortname (left column) to the full git URL. `amc package search`
# also reads it for browse + version listing + compat check.
#
# Two tiers:
#   - tier = "official"  → hosted under amalgame-lang/ org,
#                           maintained by Bastien Mouget directly.
#                           Carries the strongest trust signal.
#   - tier = "listed"    → hosted at a third-party URL, verified
#                           via PR review on this repo. Code stays
#                           with the upstream author; the listing
#                           is a "yes, I've reviewed this and it's
#                           reasonable" stamp, not an ownership
#                           transfer.
#
# Unlisted packages always work via the full URL form:
#   `amc package add github.com/foo/amalgame-bar@v1.0.0`
# — but get an "⚠ unverified" warning at install time.
#
# ── Schema (since amc v0.5.5) ───────────────────────────
#
# Two flat arrays:
#
#   [[package]]    — one entry per shortname (4 lines + meta).
#   [[version]]    — one entry per (shortname, tag) pair. Linked
#                    to [[package]] by the `package` field. Each
#                    version declares the manifest's
#                    `required-amalgame` so `amc package search`
#                    can mark compat per tag without cloning.
#
# To submit a new version (own package): open a PR adding one
# [[version]] block. To submit a new package: add both a
# [[package]] and at least one [[version]].
#
# Review criteria for new packages:
#   - The repo exists, builds, and tests pass via its own CI.
#   - The manifest declares a non-empty `license` field
#     compatible with Apache-2.0 redistribution.
#   - The repo's README is honest about what the package does.
#   - The author isn't impersonating an existing project.
#
# Removal: open a PR removing the entry. Reasons we'd remove:
#   - Repo deleted / 404
#   - License changed to something incompatible
#   - Maintainer asks
#   - Discovered to be malware

schema-version = 1

# ─────────────────────────────────────────────────────────
#  Packages
# ─────────────────────────────────────────────────────────

[[package]]
name        = "sqlite"
url         = "github.com/amalgame-lang/amalgame-database-sqlite"
description = "SQLite 3 binding via vendored amalgamation. Public-domain upstream, no libsqlite3-dev needed."
tier        = "official"
maintainer  = "amalgame-lang"
license     = "Apache-2.0"
category    = "database"

[[package]]
name        = "redis"
url         = "github.com/amalgame-lang/amalgame-database-nosql-redis"
description = "Redis (RESP2) client over raw TCP. Pure-protocol, no vendored lib. Works against Redis / KeyDB / Dragonfly / Valkey."
tier        = "official"
maintainer  = "amalgame-lang"
license     = "Apache-2.0"
category    = "database"

[[package]]
name        = "mqtt"
url         = "github.com/amalgame-lang/amalgame-messaging-mqtt"
description = "MQTT 3.1.1 client over raw TCP. Pure-protocol, no vendored lib. QoS 0 in v1."
tier        = "official"
maintainer  = "amalgame-lang"
license     = "Apache-2.0"
category    = "messaging"

[[package]]
name        = "duckdb"
url         = "github.com/amalgame-lang/amalgame-database-duckdb"
description = "DuckDB binding — vendored C++ amalgamation (MIT). Embedded analytical (OLAP) database; SQLite for analytics."
tier        = "official"
maintainer  = "amalgame-lang"
license     = "Apache-2.0"
category    = "database"

[[package]]
name        = "encoding"
url         = "github.com/amalgame-lang/amalgame-encoding"
description = "Pure-Amalgame encoding facade: Base64 (RFC 4648), Hex, percent-encoding (RFC 3986). Stateless static helpers."
tier        = "official"
maintainer  = "amalgame-lang"
license     = "Apache-2.0"
category    = "framework"

[[package]]
name        = "crypto"
url         = "github.com/amalgame-lang/amalgame-crypto"
description = "Pure-Amalgame crypto facade: SHA-256 (FIPS 180-4) + HMAC-SHA-256 (RFC 2104)."
tier        = "official"
maintainer  = "amalgame-lang"
license     = "Apache-2.0"
category    = "framework"

[[package]]
name        = "random"
url         = "github.com/amalgame-lang/amalgame-random"
description = "Pure-Amalgame random facade: PCG-32 deterministic stream + crypto-grade entropy from OS primitives."
tier        = "official"
maintainer  = "amalgame-lang"
license     = "Apache-2.0"
category    = "framework"

[[package]]
name        = "math"
url         = "github.com/amalgame-lang/amalgame-math"
description = "Pure-Amalgame Math facade: sqrt/sin/cos/pow/log + constants + integer math + IsPrime + float checks."
tier        = "official"
maintainer  = "amalgame-lang"
license     = "Apache-2.0"
category    = "framework"

[[package]]
name        = "math-vec"
url         = "github.com/amalgame-lang/amalgame-math-vec"
description = "Pure-Amalgame Vec3/Vec4/Mat4 — column-major 4×4 matrix math (OpenGL convention) for graphics + games."
tier        = "official"
maintainer  = "amalgame-lang"
license     = "Apache-2.0"
category    = "framework"

[[package]]
name        = "datetime"
url         = "github.com/amalgame-lang/amalgame-datetime"
description = "Pure-Amalgame datetime facade: Instant (nanosecond UTC) + Duration (signed i64 ns) + Stopwatch (monotonic) + ISO 8601 format/parse."
tier        = "official"
maintainer  = "amalgame-lang"
license     = "Apache-2.0"
category    = "framework"

[[package]]
name        = "logging"
url         = "github.com/amalgame-lang/amalgame-logging"
description = "Pure-Amalgame logging facade: 4 levels (Debug/Info/Warn/Error) + UTC ISO 8601 timestamps + optional file sink."
tier        = "official"
maintainer  = "amalgame-lang"
license     = "Apache-2.0"
category    = "framework"

[[package]]
name        = "service"
url         = "github.com/amalgame-lang/amalgame-service"
description = "Pure-Amalgame service facade: SIGTERM/SIGINT handler + ShouldStop polling + interruptible Sleep for long-running daemons."
tier        = "official"
maintainer  = "amalgame-lang"
license     = "Apache-2.0"
category    = "framework"

[[package]]
name        = "io-filewatcher"
url         = "github.com/amalgame-lang/amalgame-io-filewatcher"
description = "Pure-Amalgame file-watch facade: single-file mtime polling (Exists / Changed / GetPath). Cross-platform via stat() / _stat64."
tier        = "official"
maintainer  = "amalgame-lang"
license     = "Apache-2.0"
category    = "framework"

[[package]]
name        = "yaml"
url         = "github.com/amalgame-lang/amalgame-yaml"
description = "Pure-Amalgame YAML 1.2 subset reader: block mappings + sequences + scalars (bool/int/float/string)."
tier        = "official"
maintainer  = "amalgame-lang"
license     = "Apache-2.0"
category    = "framework"

[[package]]
name        = "regex"
url         = "github.com/amalgame-lang/amalgame-regex"
description = "POSIX extended-regex binding: Test / Match / MatchAll / Replace. Available on every POSIX + MSYS2."
tier        = "official"
maintainer  = "amalgame-lang"
license     = "Apache-2.0"
category    = "framework"

[[package]]
name        = "compress"
url         = "github.com/amalgame-lang/amalgame-compress"
description = "zlib gzip + raw-deflate codec: Gzip / Gunzip / Deflate / Inflate over List<int> byte buffers."
tier        = "official"
maintainer  = "amalgame-lang"
license     = "Apache-2.0"
category    = "framework"

[[package]]
name        = "net-websocket"
url         = "github.com/amalgame-lang/amalgame-net-websocket"
description = "RFC 6455 WebSocket client: text frames, plain TCP, in-process SHA-1 + Base64 (no OpenSSL needed)."
tier        = "official"
maintainer  = "amalgame-lang"
license     = "Apache-2.0"
category    = "framework"

[[package]]
name        = "ui-sdl"
url         = "github.com/amalgame-lang/amalgame-ui-sdl"
description = "Thin SDL2/SDL3 binding — Window/Event/Surface/Font/Color/Rect/OSTheme. Foundation for amalgame-ui-forms."
tier        = "official"
maintainer  = "amalgame-lang"
license     = "Apache-2.0"
category    = "ui"

[[package]]
name        = "ui-forms"
url         = "github.com/amalgame-lang/amalgame-ui-forms"
description = "Retained-mode GUI toolkit on top of ui-sdl: Form + 8 widget kinds + Stack/Grid/Absolute layouts + light/dark theming."
tier        = "official"
maintainer  = "amalgame-lang"
license     = "Apache-2.0"
category    = "ui"

# ─────────────────────────────────────────────────────────
#  Versions
# ─────────────────────────────────────────────────────────
#
# Ordered newest-last so `amc package search` can pick the
# latest by scanning to the end. Anything outside this list
# is invisible to shortname `add` — fall back to the full
# URL form for unindexed / pre-release tags.

[[version]]
package           = "sqlite"
tag               = "v0.2.0"
required-amalgame = ">=0.5.0"

[[version]]
package           = "redis"
tag               = "v0.2.0"
required-amalgame = ">=0.5.0"

[[version]]
package           = "mqtt"
tag               = "v0.2.0"
required-amalgame = ">=0.5.0"

[[version]]
package           = "duckdb"
tag               = "v0.1.0"
required-amalgame = ">=0.5.3"

[[version]]
package           = "duckdb"
tag               = "v0.1.1"
required-amalgame = ">=0.5.4"

[[version]]
package           = "sqlite"
tag               = "v0.2.2"
required-amalgame = ">=0.5.4"

[[version]]
package           = "encoding"
tag               = "v0.1.0"
required-amalgame = ">=0.7.6"

[[version]]
package           = "crypto"
tag               = "v0.1.0"
required-amalgame = ">=0.7.6"

[[version]]
package           = "random"
tag               = "v0.1.0"
required-amalgame = ">=0.7.6"

[[version]]
package           = "math"
tag               = "v0.1.0"
required-amalgame = ">=0.7.6"

[[version]]
package           = "math-vec"
tag               = "v0.1.0"
required-amalgame = ">=0.7.6"

[[version]]
package           = "logging"
tag               = "v0.1.0"
required-amalgame = ">=0.7.7"

[[version]]
package           = "service"
tag               = "v0.1.0"
required-amalgame = ">=0.7.7"

[[version]]
package           = "io-filewatcher"
tag               = "v0.1.0"
required-amalgame = ">=0.7.7"

[[version]]
package           = "yaml"
tag               = "v0.1.0"
required-amalgame = ">=0.7.7"

[[version]]
package           = "datetime"
tag               = "v0.1.0"
required-amalgame = ">=0.7.7"

[[version]]
package           = "regex"
tag               = "v0.1.0"
required-amalgame = ">=0.7.7"

[[version]]
package           = "compress"
tag               = "v0.1.0"
required-amalgame = ">=0.7.7"

[[version]]
package           = "net-websocket"
tag               = "v0.1.0"
required-amalgame = ">=0.7.7"

[[version]]
package           = "ui-sdl"
tag               = "v0.1.0"
required-amalgame = ">=0.8.7"

[[version]]
package           = "ui-forms"
tag               = "v0.1.0"
required-amalgame = ">=0.8.7"

[[version]]
package           = "ui-forms"
tag               = "v0.1.1"
required-amalgame = ">=0.8.7"

[[version]]
package           = "ui-forms"
tag               = "v0.1.2"
required-amalgame = ">=0.8.7"

[[version]]
package           = "ui-forms"
tag               = "v0.1.3"
required-amalgame = ">=0.8.7"

[[version]]
package           = "ui-forms"
tag               = "v0.1.4"
required-amalgame = ">=0.8.10"
